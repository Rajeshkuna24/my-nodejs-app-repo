name: sync up to codecommit

on:
  push:
    tags-ignore:
      - '*'
    branches:
      - '*'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1.0.3
        with:
          aws-access-key-id: AKIA6LEM665OWW53CCMP
          aws-secret-access-key: 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
          aws-region: us-east-1

      - name: Sync up to CodeCommit
        uses: tsgit18/sync-up-to-codecommit-action@v1
        with:
          repository_name: my-nodejs-app-repo
          aws_region: ap-south-1
